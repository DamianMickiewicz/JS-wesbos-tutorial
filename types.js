/*
const name = 'wes';
const middle = "topher";
const last = `bos`;

const hello = `My name is ${name}. Nice to meet you.`;

const html = `
    <div>
        <h2>${name}</h2>
        <p>${hello}</p>
    </div>
`;

document.body.innerHTML = html;

const age = 100;
const name = `wes`;

// const a = 20;
// const b = 30;

const smarties = 20;
const kids = 3;
const eachKidsGets = Math.floor(smarties / kids);

console.log(`Each kids gets ${eachKidsGets}`);


const person = {
    first: 'wes',
    last: 'bos',
    age: 100,
};


let somethingUndefined;
const somethingNull = null;

const cher = {
    name: 'cher',
};

const teller = {
    name: 'Raymond',
    last: 'Teller',
};

teller.name = "Teller";
teller.last = null;

*/

let isDrawing = false;
let age = 18;
const ofAge = age > 19;
console.log(ofAge);

age = 100;
let age2 = 100;

// uzycie == sprawdza wartosc
// uzycie === sprawdza wartosc i typ
